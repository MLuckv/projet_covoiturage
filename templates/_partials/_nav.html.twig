<nav class="navbar navbar-expand-lg navbar-light bg-light">
	<a class="navbar-brand" href="{{ path('app_home') }}">Accueil</a>
	<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
		<span class="navbar-toggler-icon"></span>
	</button>

	<div class="collapse navbar-collapse" id="navbarSupportedContent">
		<ul class="navbar-nav mr-auto">
			<li class="nav-item active">
				<a class="nav-link ml-2" href="{{ path('app_travel') }}"><stong>Liste Voyage</strong>
					<span class="sr-only">(current)</span>
				</a>
			</li>
			{% if app.user %}
				{% if app.user.isDriver == true %}
				<li class="nav-item active">
					<a class="nav-link" href="{{ path('app_voyage') }}">Vos Voyages <small>(ajouter/voir)</small></a>
				</li>
				<li class="nav-item active">
					<a class="nav-link" href="{{ path('car_index') }}">Vos vehicule <small>(ajouter/voir)</small></a>
				</li>
				{% else %}
				<li class="nav-item active">
					<a class="nav-link" href="{{ path('app_driver') }}">Devenir conducteur</a>
				</li>	
				{% endif %}
			{% endif %}
		</ul>

		<ul class="navbar-nav me-3 mb-2 mb-lg-0">
			{% if app.user %}
				<li class="nav-item">
					<a class="nav-link" href="{{ path('app_messages') }}">Messagerie</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="{{ path('profile_user', {slug: app.user.slug}) }}">Profil</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="{{ path('app_logout') }}">Déconnexion</a>
				</li>
			{% else %}
				<li class="nav-item">
					<a class="nav-link" href="{{ path('app_login') }}">Connexion</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="{{ path('app_register') }}">Inscription</a>
				</li>
			{% endif %}
		</ul>
		<img src="{{asset('/assets/img/baniere.png')}}" width="214" height="70" class="form-inline my-2 my-lg-0">
	</div>
</nav>
