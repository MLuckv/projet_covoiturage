{% extends 'base.html.twig' %}

{% block title %}Devenir conducteur
{% endblock %}


{% block body %}

	{{ form_start(form) }}

	<div class="container mt-3 mb-2">
		<div class="row">
			<h5>Ville</h5>
			{{form_row(form.ville_user)}}
		</div>

		<div class="row mb-3">
			<h5>Age</h5>
			{{form_row(form.age)}}</div>
		<div class="row">{{form_row(form.submit)}}</div>
	</div>
	{{ form_end(form) }}

{% endblock %}
