{% extends 'base.html.twig' %}

{% block title %}vehicule{% endblock %}

{% block body %}
<div class="container">
		<div class="row-12 my-2">
			<h2>Vos vehicules</h2>
		</div>
		<div class="my-3">
				<div class="row-6">
					<a href="{{ path('car_list') }}" class="btn btn-primary">Liste de vos vehicule</a>
				
				<a href="{{ path('car_add') }}" class="btn btn-secondary">Ajouter un vehicule</a>
			</div>
		</div>
	</div>
{% endblock %}
