{% extends 'base.html.twig' %}

{% block title %}Profile -
	{{user.lastname}}
{% endblock %}

{% block body %}

	<div class="container">
		<div class="row mt-3 mb-2 text-center">
			<h5 class="">
                {{user.lastname}} {{user.firstname}}
            </h5>
        </div>
        <div class="row mt-3 mb-2 text-left">
			<h6>Contact :
				{{user.email}}<br>{{user.numTel}}
            </h6>
		</div>
	</div>
    

{% endblock %}
